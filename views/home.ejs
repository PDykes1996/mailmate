<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Select Email Template</title>
  <style>
    /* General Styles */
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0;
      padding: 20px;
    }
    
    h1 {
      font-size: 1.8em;
      color: #333;
      margin-bottom: 20px;
    }

    /* Button Container */
    #buttons {
      width: 100%;
      max-width: 400px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    /* Button Styles */
    #buttons a {
      text-decoration: none;
    }

    #buttons button {
      width: 100%;
      padding: 15px;
      font-size: 1em;
      font-weight: bold;
      color: #333;
      background-color: #e8eaf6;
      border: 1px solid #ccc;
      border-radius: 8px;
      transition: background-color 0.3s, color 0.3s, border-color 0.3s;
      cursor: pointer;
    }

    #buttons button:hover {
      background-color: #7986cb;
      color: #fff;
      border-color: #7986cb;
    }
  </style>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Select Email Template</title>
</head>
<body>
  <h1>Select an Email Template</h1>
  <div id="buttons">
    <% textFiles.forEach(file => { %>
        <a href="/parseTemplate/<%= file %>">
        <button><%= file %></button>
        </a>
    <% }) %>
  </div>
  <h1>Add Custom Template</h1>
  <div id="buttons">
      <a href ="/upload">
        <button>Add Existing Template</button>
      </a>
  </div>
</body>
</html>
</body>
</html>